{
  "name": "cypress_try",
  "version": "1.0.0",
  "description": "cypress simple test",
  "main": "index.js",
  "scripts": {
    "cy:open":"cypress open",
    "cy:run":"cypress run",
    "cy:run:spec":"cypress:run --spec",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cypress": "^9.5.2"
  },
  "devDependencies": {
    "typescript": "^4.6.2"
  }
}
